# Default Convex deployment used by the frontend build.
# `npx convex dev` writes a .env.local next to this file which takes precedence locally.
VITE_CONVEX_URL=https://clear-loris-51.eu-west-1.convex.cloud
